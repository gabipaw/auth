{
  "name": "auth",
  "version": "1.0.0",
  "private": true,
  "description": "Logowanie i sesja na Supabase Auth (rejestracja, logowanie, trasa chroniona, wylogowanie) + testy e2e",
  "scripts": {
    "serve": "python3 -m http.server 4173",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.0"
  }
}
